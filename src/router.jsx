import * as React from "react"

import { Index } from './pages/index'

function RouterView() {
    if props.
}


type Option = {
    routes: Array<{
        path: string
        layout: React.FC
        page: React.ReactNode
    }>
}

const routes = [{
    path: "/",
    component: Index
}]